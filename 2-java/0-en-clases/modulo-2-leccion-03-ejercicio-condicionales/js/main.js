"use strict";
const name = "yanelis";
const profesion = "profe";

if (profesion === "profe") {

    
    if (name === "dayana" || name === "yanelis") {
        console.log("Bienvenida " + name);
    } 
} else {console.log ("Eres una estudiante")}

if((name === "dayana" || name === "yanelis") && profesion === "profe") {console.log("Bienvenida " + name);}